#include <stdio.h>
#include <string.h>
int main () {
 char ball[][50] = {"красный", "синий", "зеленый"};
    strcpy(ball[0], "жёлтый");
    for (int i = 0;i < 3; i++)
    {
        printf("%s\n", ball[i]);
    }
}
