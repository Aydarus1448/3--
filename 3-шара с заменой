#include <stdio.h>
#include <stdio.h>

int main() {
    const char *balls[3] = {
        "красный",
        "синий",
        "зеленый"
    };

    balls[0] = "желтый";

    for (int i = 0; i < 3; i++) {
        printf("%s\n", balls[i]);
    }

    return 0;
}
